// Authentication temporarily disabled for deployment testing
// Will re-enable after ALB and backend connectivity is working
export default function middleware() {
  return;
}

export const config = {
  matcher: [],
};